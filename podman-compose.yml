services:
  app:
    build:
      context: .
      dockerfile: Containerfile
    container_name: mockup-gen
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./output:/app/output
    env_file: .env.production
    restart: always
